<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Recognize Fe Test App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="mobile-web-app-capable" content="yes" />
    <%= csrf_meta_tags %> <%= csp_meta_tag %>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <%= vite_client_tag %> <%= vite_stylesheet_tag 'application' %> <%=
    vite_javascript_tag 'application' %>
  </head>
  <body>
    <nav
      class="navbar navbar-expand-sm navbar-light bg-light fixed-top border-bottom px-3"
    >
      <a class="navbar-brand" href="/">Recognitions</a>
      <div class="ms-auto d-flex gap-2 align-items-center">
        <% if defined?(logged_in?) && logged_in? %>
        <span class="small text-muted">Hi, <%= current_user.name %></span>
        <%= button_to 'Logout', logout_path, method: :delete, class: 'btn
        btn-outline-secondary btn-sm' %> <% end %>
      </div>
    </nav>
    <main>
      <div
        id="toast-container"
        class="position-fixed top-0 end-0 p-3"
        style="z-index: 1080"
      >
        <% flash.each do |k, v| %> <%= render 'shared/toast', kind: k, message:
        v %> <% end %>
      </div>

      <div class="app-container">
        <%= render 'shared/sidebar' %>

        <div class="app-main"><%= yield %></div>
      </div>
    </main>
  </body>
</html>
